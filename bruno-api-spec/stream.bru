meta {
  name: stream
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/stream
  body: none
  auth: inherit
}

headers {
  Accept: text/event-stream
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

settings {
  encodeUrl: true
  timeout: 0
}
